export interface NotFoundDictionary {
  /** Shown in the browser tab and as the page's meta title. */
  "page-title": string;
  "page-description": string;
  /** The oversized numeral above the heading. */
  code: string;
  title: string;
  description: string;
  "home-label": string;
  "writing-label": string;
}
